import React from "react";
import { StLoadingContainer, StLoadingSpinner } from "./style";

const Loading = () => {
  return (
    <StLoadingContainer>
      <StLoadingSpinner />
    </StLoadingContainer>
  );
};

export default Loading;
